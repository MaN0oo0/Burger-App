<div class="row col-md-12 d-flex justify-content-center">
    <div class="col-md-6 my-2 mx-2">
        {{#if login}}
        {{> login}}
        {{else}}
        {{> register}}
        {{/if}}
    </div>
</div>